{
  "name": "sample_cloudtrail_with_glue",
  "version": "0.1.0",
  "bin": {
    "sample_cloudtrail_with_glue": "bin/sample_cloudtrail_with_glue.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy --require-approval=never",
    "lint": "eslint . --ext .ts,.tsx --report-unused-disable-directives --max-warnings=0 --fix --ignore-pattern '**/*.d.ts'",
    "clean": "rm -rf node_modules package-lock.json build cdk.out",
    "dev": "npm run clean && npm i && npm run lint && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "aws-cdk": "^2.1024.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3",
    "eslint": "^9.27.0",
    "@eslint/js": "^9.27.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "eslint-plugin-jest": "^28.0.0",
    "globals": "^15.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.210.0",
    "constructs": "^10.0.0",
    "@aws-cdk/aws-glue-alpha": "^2.210.0-alpha.0"
  }
}
